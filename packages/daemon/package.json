{
  "name": "agentpilot-daemon",
  "version": "1.0.0",
  "description": "AgentPilot daemon — intercept LLM calls and stream usage to mobile",
  "type": "module",
  "bin": {
    "agentpilot-daemon": "./dist/daemon.js"
  },
  "scripts": {
    "dev": "bun --watch src/daemon.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "commander": "^12.0.0",
    "ws": "^8.16.0"
  },
  "devDependencies": {
    "@types/ws": "^8.5.10",
    "bun-types": "latest",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
